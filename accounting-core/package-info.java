/**
 * Core accounting engine of Accounting-lib.
 *
 * <p>This package contains domain logic, aggregates, use-cases,
 * services, policies, and validation logic.
 *
 * <p>It depends only on minimal stable libraries (Moneta, SLF4J, Commons Lang)
 * and should not reference any framework-specific code.
 */
package org.accountinglib.core;